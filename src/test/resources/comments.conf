service1: {
  url: /*typesafe config doesn't allow inline comments, but we do!*/ "https://hostdomain.com/service1/v1"
  timeout: 1s // double slash comments are fine
  # hash comments are fine too
}

/**
* block comments are also fine, although the syntax highlighting in your editor probably won't like it
*/

port: 8080